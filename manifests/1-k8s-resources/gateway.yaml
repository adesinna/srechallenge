apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: dumbkv-gateway
  namespace: default
spec:
  gatewayClassName: nginx
  listeners:
    - name: http
      protocol: HTTP
      port: 80
      allowedRoutes:
        namespaces:
          from: All
    - name: https
      protocol: HTTPS
      port: 443
      tls:
        mode: Terminate
        certificateRefs:
          - name: letsencrypt        # cert-manager secret
            kind: Secret
      allowedRoutes:
        namespaces:
          from: All
