apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - svc.yaml
  - deployment.yaml
  - pvc.yaml
labels:
  - pairs:
      app: dumbkv-app
    includeSelectors: true
#components:
#    - ../../components/